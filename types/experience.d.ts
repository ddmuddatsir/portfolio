export interface Experience {
  company: string;
  location: string;
  role: string;
  period: string;
  tasks: string[];
}

export interface LearnStep {
  icon: string;
  years: string;
  text: string;
}
